# Launcher branding implementation

The Android launcher icon is generated at build time from `ic_launcher.webp.b64`, which contains the approved no-shield TrailCharter landscape artwork. The build produces legacy and adaptive launcher resources without redrawing or reinterpreting the approved composition.
